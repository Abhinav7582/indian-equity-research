"""Research computations.

Phase 1.5 scope: the H4 regime experiment, run against published index series.
No stock-level data, no corporate actions, no universe reconstruction.

Everything here is deterministic and pure: given the same inputs it returns the
same outputs, with no I/O and no hidden state. That is what makes a result
reproducible from a commit hash.
"""
